import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Transaction Entry",
};

const TransactionEntry = () => {
  return <div>TransactionEntry</div>;
};

export default TransactionEntry;
